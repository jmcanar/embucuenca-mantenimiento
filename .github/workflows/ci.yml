name: CI EmbuCuenca

on: [push, pull_request]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Verificar que el pipeline corre
        run: echo "Pipeline de CI ejecutado correctamente para el módulo de Órdenes de Trabajo de EmbuCuenca"
